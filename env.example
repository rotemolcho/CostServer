# Port for the Express server
PORT=3000

# MongoDB connection URI (replace <username>,<password>,<cluster-url>,<dbname>)
MONGO_URI="mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority"
